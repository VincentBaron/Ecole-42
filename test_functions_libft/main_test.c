int main(void)
{
    
    char *str_test[]= {"lol",""}
    char *str_cpy[] = {"lal",""}
    int i;
    
    while (i < ac)
    {
    if (ft_strcmp(str_test[i], str_cpy[i]) == strcmp(str_))
    
    if (test_memset(av[1], atoi(av[ac -1]), strlen(av[1]))))
    if (test_strcpy(av[1], av[2]))
    
}

av = {"lol", "flume", "toto", "1", "2"}
./a.out lol flume toto 1 2
